{{#if error}}
       <div class="error"> Oops! something went wrong</div>
   {{/if}}
<div class="container">
    <div class="content" id="profileContent">
        <div class="textDiv">
            <div class="ribbon custom-profile">
                <h1>PROFILE YOURSELF.</h1>
            </div>
            <div class="ribbon custom-profile">
                <div class="info">
                    <p>Entertain us by providing some general information on your person.</p>
                </div>
            </div>
        </div>

        <form method="POST" class="inputField" >

            <input type="hidden" name="_csrf" value="{{csrfToken}}">

            <div class="textInput">
                <div class="input custom-profile">
                    <br>
                    <input type="number" name="age" id="age" autocomplete="off" placeholder="   AGE">
                </div>
                <div class="input custom-profile">
                    <br>
                    <input type="text" name="city" id="city" autocomplete="off" placeholder="CITY">
                </div>
                <div class="input custom-profile">
                    <br>
                    <input type="text" name="homepage" id="homepage" autocomplete="off" placeholder="HOMEPAGE">
                </div>

            </div>
            <button name="button" id="submit" class="profile-button">CONTINUE</button>

        </form>
    </div>

</div>
